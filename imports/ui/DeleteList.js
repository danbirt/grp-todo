import React from 'react';

export default class DeleteList extends React.Component {
    render() {
        return(
            <button className="button button--pill" onClick={() => {}}>Delete</button>
        );
    }
}